- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim/.ycm_extra_conf.py: ycm_extra_conf.py
    ~/.vimrc: vimrc
    ~/.inputrc: inputrc

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
    - [git submodule update --init --recursive, Installing submodule]
